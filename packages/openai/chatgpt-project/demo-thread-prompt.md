# ChatGPT Project Demo Thread Prompt

Use the uploaded public-safe project files to explain Agent Librarian in
plain language.

Then review the uploaded generated catalog outputs:

```text
examples/generated-catalog/catalog.md
examples/generated-catalog/diagnostics.json
examples/generated-catalog/overlap-report.json
```

State clearly that this ChatGPT Project is advisory and does not run the local
CLI by default. If validation or report evidence is missing, ask me to run:

```bash
agent-librarian validate examples/generated-catalog
agent-librarian report examples/generated-catalog
```

Summarize only uploaded or pasted deterministic evidence. Do not certify
safety, privacy, correctness, completeness, approval, compliance, or
publication readiness.

